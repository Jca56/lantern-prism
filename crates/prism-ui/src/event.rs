//! Input events. Defined in `prism-ops::input` (the keymap needs them below
//! the UI); re-exported here so UI code reads naturally.

pub use prism_ops::input::{Event, Key, Modifiers, MouseButton, WheelDelta};
